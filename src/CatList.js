import React from 'react';

const CatList = ({catPics}) => {
    // const renderCatImages = catsPics.map((pic, index) => <img src={pic.url} key={index}/>);
   
    return(
        <div>
       
        </div>
    )
}

export default CatList