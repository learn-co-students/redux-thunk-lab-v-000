import React from 'react';

const CatList = ({ catPics }) => {
  // const renderCats = catPics.map(pic => <img src={pic.url} />);
  // {renderCats}
  return (
    <div>
      <h1>CAT LIST</h1>
    </div>
  );
};

export default CatList
